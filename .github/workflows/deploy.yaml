name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  repository_dispatch:
    types:
      - vault-updated
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout quartz repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Clone vault
        env:
          VAULT_PAT: ${{ secrets.VAULT_PAT }}
        run: git clone --depth 1 "https://$VAULT_PAT@github.com/nickpettican/pramana-wiki-vault.git" _vault

      - name: Copy wiki content
        run: |
          [ -d "_vault/wiki" ] || { echo "::error::_vault/wiki not found in vault repo"; exit 1; }
          mkdir -p content
          cp -r _vault/wiki/. content/
          rm content/log.md

      - name: Remove vault clone
        if: always()
        run: rm -rf _vault

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies
        run: npm ci

      - name: Build Quartz
        run: npx quartz build

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: public

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
